# Scripts legacy (fuera del path crítico del chat tool)

- `diff_actuaciones.py` — compara dos snapshots de `actuaciones.json` (base para un futuro modo watchlist).
- `publicaciones_poc.py` — PoC exploratorio contra publicacionesprocesales.ramajudicial.gov.co (estados/fijaciones).

No se importan desde `service/` y no forman parte del contrato del tool.
